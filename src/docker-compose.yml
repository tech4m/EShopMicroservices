version: '3.4'

services:
  
  catalogdb:
    image: postgres

  basketdb:
    image: postgres

  messagebroker:
    image: rabbitmq:management

  distributedcache:
    image: redis

  orderdb:
    image: mcr.microsoft.com/mssql/server

volumes:
  postgres_catalog:
  postgres_basket:  